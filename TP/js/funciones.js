function saludar(nombre) {
    alert("Hola " + nombre)
}

function resultado(numero1, numero2) {
    alert("el resultado es: " + (numero1 + numero2))
}

function numero(n) {
    const aux = 72;

    if(aux > n) {
        alert ("el número " + n +" es menor que el auxiliar " + aux)
    }
    else if(aux < n) {
        alert ("el número " + n + " es mayor que el auxiliar " + aux)
    }
}

